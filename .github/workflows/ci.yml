name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    # glassEffect needs the macOS 26 SDK.
    runs-on: macos-26
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - run: xcrun swift --version
      - run: make test
      - name: Build the app bundle
        run: |
          make app
          plutil -lint RoamRun.app/Contents/Info.plist
          codesign --verify --strict RoamRun.app
          plutil -extract CFBundleVersion raw Info.plist | grep -Eq '^[0-9]+$'
          RoamRun.app/Contents/MacOS/RoamRun --help > /dev/null
          RoamRun.app/Contents/MacOS/RoamRun init --print | head -2 | grep -qx 'name: roamrun'   # init/--uninstall rely on this header
      - name: Build the screenshot (SNAPSHOT) variant
        run: xcrun swift build -Xswiftc -DSNAPSHOT
